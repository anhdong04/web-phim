name: Web Phim v6.4.3 K20 VN Mirror
on:
  push:
    paths:
      - 'v642_patch.js'
      - 'addon_v642.js'
      - 'package.json'
      - '.github/workflows/ci-v642.yml'
  workflow_dispatch:

jobs:
  vn-bridge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Syntax check
        run: node --check v642_patch.js && node --check addon_v642.js
      - name: Static regression
        run: |
          node <<'NODE'
          const fs=require('fs');
          const s=fs.readFileSync('v642_patch.js','utf8');
          for (const needle of [
            "'/vn/manifest.json'",
            "'stp:'",
            "'hh3d:'",
            "'clbpx:'",
            "V642_CATALOG_PREFIXES",
            "K20-VN manifest",
            "https://sc.k-20.xyz"
          ]) if (!s.includes(needle)) throw new Error('missing ' + needle);
          console.log('v6.4.3 K20 mirror assertions passed');
          NODE
      - name: Local manifest smoke
        env:
          PORT: 7000
          HH3D_PIKPAK_ENABLED: 'false'
          YANHH3D_ENABLED: 'false'
        run: |
          node addon_v642.js >/tmp/v642.log 2>&1 &
          pid=$!
          ok=0
          for i in {1..40}; do
            if curl -fsS http://127.0.0.1:7000/vn/manifest.json >/tmp/vn.json; then ok=1; break; fi
            sleep 0.5
          done
          if [ "$ok" != "1" ]; then cat /tmp/v642.log; exit 1; fi
          node <<'NODE'
          const j=require('/tmp/vn.json');
          if (j.version !== '6.4.3') throw new Error('VN manifest version ' + j.version);
          const ids=(j.catalogs||[]).map(c=>c.id);
          if (!ids.some(id=>String(id).startsWith('stp-'))) throw new Error('STP missing');
          if (!ids.some(id=>String(id).startsWith('hh3d-'))) throw new Error('K20 HH3D missing');
          if (!ids.some(id=>String(id).startsWith('clbpx-'))) throw new Error('CLBPX missing');
          if (!(j.idPrefixes||[]).includes('hh3d:')) throw new Error('K20 HH3D prefix missing');
          NODE
          curl -fsS http://127.0.0.1:7000/manifest.json >/tmp/root.json
          curl -fsS http://127.0.0.1:7000/full/manifest.json >/tmp/full.json
          kill -0 $pid
          kill $pid || true
